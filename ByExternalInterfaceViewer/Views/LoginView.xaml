<Window x:Class="ByExternalInterfaceViewer.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ByExternalInterfaceViewer.ViewModels"
        mc:Ignorable="d"
        Title="Login" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="true"
        MouseDown="Window_MouseDown">
    
    
    <Grid Background="#FF211B1B">
        <Grid.RowDefinitions>
            <RowDefinition Height="180"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Height="103" Width="86" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Image Source="pack://application:,,,/Images/2025-12-31_15h48_39.png" Width="86" Height="103" VerticalAlignment="center" />
        </StackPanel>
        <TextBlock Text="V:" FontSize="12" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="250 10 0 0"/>
        <TextBlock Text="{Binding SwVersion}" FontSize="12" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="300 10 0 0"/>
        <TextBlock Text="External Interface viewer" FontSize="30" Foreground="White" HorizontalAlignment="Center" Margin="0 120 0 0" />


        <StackPanel Grid.Row="1">
            <TextBlock Text="Username" FontSize="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 30 120 0"  />
            <ComboBox ItemsSource="{Binding Username}" SelectedItem="{Binding SelectedUserName}" VerticalAlignment="Center" Margin="0 10" Width="200" Foreground="Black"/>

            <TextBlock Text="Password" FontSize="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 40 120 0"  />
            <PasswordBox Name="Passwordbox" PasswordChanged="Passwordbox_PasswordChanged" Margin="0 10 0 0 " Width="200">
            </PasswordBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Login" Command="{Binding LoginCommand}" Style="{StaticResource ButtonStyle}" Height="50" Width="100" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 0 40 0"/>
            <Button Content="Close" Command="{Binding CloseLoginViewCommand}" Style="{StaticResource ButtonStyle}" Height="50" Width="100" VerticalAlignment="Center" HorizontalAlignment="Center"/>

        </StackPanel>





    </Grid>
</Window>
